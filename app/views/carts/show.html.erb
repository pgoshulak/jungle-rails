<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <% if cookies[:cart] == nil || cookies[:cart] == "{}" %>
    <%= render 'cart_no_items' %>
  <% else %>
    <%= render 'cart_with_items' %>
  <% end %>

</section>
